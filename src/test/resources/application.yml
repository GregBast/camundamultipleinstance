camunda:
  bpm:
    admin-user:
      id: test
      password: test
      firstName: test
    database:
      type: h2

spring:
  datasource:
    url: jdbc:h2:mem:mydb;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    username: sa
    password: sa
    driverClassName: org.h2.Driver